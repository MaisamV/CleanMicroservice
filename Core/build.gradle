buildscript {
    repositories repos
}

apply plugin: "java"
apply plugin: "org.jetbrains.kotlin.jvm"

dependencies {
    api project(":ICore")
    api project(":IRepo")

    implementation "org.jetbrains.kotlin:kotlin-stdlib"
}

test {
    useJUnitPlatform()
}