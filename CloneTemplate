git clone https://github.com/MaisamV/CleanMicroservice.git
cd ./CleanMicroservice
git remote rename origin github
git branch -m base
git remote set-url --add --push github https://github.com/MaisamV/CleanMicroservice.git
git remote set-url --add --push github http://git.sabaolgoo.ir/m.vahidsafa/microservicetemplate.git
git branch master
git checkout master
read -p "Enter Project name in gitlab: " repository
git remote add origin http://git.sabaolgoo.ir/m.vahidsafa/$repository
git push -u origin master
cd ..
mv CleanMicroservice $repository